@using Microsoft.AspNetCore.Mvc.TagHelpers
@using SFA.DAS.Admin.Roatp.Domain.Models
@using SFA.DAS.Admin.Roatp.Web.Infrastructure
@model SFA.DAS.Admin.Roatp.Web.Models.OrganisationTypeViewModel
@{
    ViewData["Title"] = "Choose the type of organisation";
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <partial name="_validationSummary" />
        <form asp-route="@RouteNames.SelectOrganisationType" method="POST" enctype="multipart/form-data">
            <div class="govuk-form-group" id="OrganisationTypeId">
                <fieldset class="govuk-fieldset">
                    <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
                        <h1 class="govuk-heading-xl govuk-!-margin-bottom-5">
                            Choose the type of organisation for @Model.LegalName
                        </h1>
                    </legend>
                    <div esfa-validation-marker-for="@Model.OrganisationTypeId" class="govuk-form-group">
                        <span asp-validation-for="OrganisationTypeId" class="govuk-error-message"></span>
                    <div class="govuk-radios" data-module="govuk-radios">
                        @foreach (var organisationType in Model.OrganisationTypes)
                        {
                            <div class="govuk-radios__item">
                                <input name="OrganisationTypeId" class="govuk-radios__input" id="OrganisationTypeId-@organisationType.Id" type="radio" value="@organisationType.Id" @organisationType.Checked>
                                <label class="govuk-label govuk-radios__label" for="OrganisationTypeId-@organisationType.Id">
                                    @organisationType.Description
                                </label>
                            </div>
                        }
                    </div>
                    </div>
                </fieldset>
            </div>
            <div class="govuk-button-group">
                <button type="submit" id="continue" class="govuk-button" data-disable-on-submit="true">Continue</button>
            </div>
        </form>
    </div>
</div>